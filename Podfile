# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'alps-ios-app-color' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for alps-ios-app-color
  pod 'AlpsSDK', '~> 0.6'

  target 'alps-ios-app-colorTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'alps-ios-app-colorUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
